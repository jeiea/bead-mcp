{
  "lock": false,
  "fmt": { "lineWidth": 100 },
  "lint": { "rules": { "exclude": ["no-import-prefix"] } },
  "unstable": ["temporal"],
  "tasks": {
    "run": "deno run --allow-all bin/main.ts",
    "test": "deno test --allow-all"
  }
}
